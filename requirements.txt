# fourier-option-pricer — all-in-one requirements file
#
# Usage (run from the repo root after cloning):
#   pip install -r requirements.txt
#
# Installs the foureng package from your local clone in editable mode,
# together with all runtime, notebook, and test dependencies.
# For library-only use without cloning: pip install fourier-option-pricer

# ── Package (editable install from local clone) ───────────────────────────────
-e .

# ── Notebook and interactive use ──────────────────────────────────────────────
pandas>=2.0
jupyter>=1.0
ipykernel>=6.0
nbformat>=5.10

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4
pytest-cov>=4.0
hypothesis>=6.112
nbmake>=1.5
